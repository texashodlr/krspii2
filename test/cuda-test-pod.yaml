apiVersion: v1
kind: Pod
metadata:
  name: cuda-vector-add
spec:
  containers:
  - name: cuda-vector-add
    image: "nvidia/cuda:12.0.0-base"
    command: ["nvidia-smi"]
    resources:
      limits:
        nvidia.com/gpus: 1
  restartPolicy: OnFailure
